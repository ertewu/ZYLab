package zystudio.cases.prepare;

/**
 * Created by wzy on 14-12-2.
 * 测试Fragment的这几个功能：</br>
 * <li>对比getFragmentManager与getChildFragment的区别，使用getChildFragment，以及实现多个fragment加入同一个父view的case</li>
 * <li>验证多个fragment，叠加显示的case</li>
 * <li>做验证FragmentTransaction.addToBackStack(null)把当前transaction移入栈的case</li>
 *<li>验证replace可不可以当四角tab使用的case</li>
 * <li>写或者移植以前那个嵌套的Fragment的case</li>
 *
 *
 * Fragment方面有三个重点类：FragmentManager,Fragment,FragmentTransaction,要从各种不同的使用方式去实践这方面的知识
 */
public class CaseFragment {
}
